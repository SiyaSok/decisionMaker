/** @format */

import React from "react";

const DecisionResult = ({ decision }) => {
  return (
    <div>
      <h2>Decision:</h2>
      <p>{decision}</p>
    </div>
  );
};

export default DecisionResult;
